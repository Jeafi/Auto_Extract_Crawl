from crawl_commons.spiders.common_spider import *

#单独测试使用
class PolicySpider(CommonSpider):  # 需要继承scrapy.Spider类
   name= "policy_spider" # 定义蜘蛛名

#单独测试使用
class PolicySpider1(CommonSpider):  # 需要继承scrapy.Spider类
   name= "policy_spider1" # 定义蜘蛛名

#单独测试使用
class PolicySpider2(CommonSpider):  # 需要继承scrapy.Spider类
   name= "policy_spider2" # 定义蜘蛛名

#单独测试使用
class PolicySpider3(CommonSpider):  # 需要继承scrapy.Spider类
   name= "policy_spider3" # 定义蜘蛛名

#单独测试使用
class PolicySpider4(CommonSpider):  # 需要继承scrapy.Spider类
   name= "policy_spider4" # 定义蜘蛛名


#单独测试使用
class PolicySpider5(CommonSpider):  # 需要继承scrapy.Spider类
   name= "policy_spider5" # 定义蜘蛛名


#单独测试使用
class PolicySpiderjs1(CommonSpider):  # 需要继承scrapy.Spider类
   name= "policy_spider_js1" # 定义蜘蛛名


#单独测试使用
class PolicySpiderjs2(CommonSpider):  # 需要继承scrapy.Spider类
   name= "policy_spider_js2" # 定义蜘蛛名


#单独测试使用
class PolicySpiderjs3(CommonSpider):  # 需要继承scrapy.Spider类
   name= "policy_spider_js3" # 定义蜘蛛名